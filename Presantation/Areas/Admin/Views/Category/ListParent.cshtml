@model List<ParentCategoryVM>

@{
    ViewData["Title"] = "ListParent";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">
            <div class="card">
                <div class="card-header">
                    <h4>Parent Category List</h4>
                </div>
                <div class="card-body">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Name</th>
                                <th>Update</th>
                                <th>Delete</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td>@item.Name</td>
                                    <td>
                                        <a asp-action="UpdateParent" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Update</a>
                                    </td>
                                    <td>
                                        <a asp-action="DeleteParent" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="card-footer">
                    <a asp-action="CreateParent" class="btn btn-primary">Create New One</a>
                </div>
            </div>
        </div>
    </div>
</div>
