@model ProductVM


@{
    ViewData["Title"] = "Details";
}

<div class="row">
    <div class="col-md-4">
        <img src="/media/İmages/@Model.ImagePath" width="400" height="600" class="img-fluid rounded ">
    </div>
    <div class="col-md-4">
        <div class="row">
            <div class="col-md-12">
                <h2>@Model.ProductName</h2>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col-md-12">
                <h4 id="product-price">@Model.Price.ToString("C2")</h4>
            </div>
        </div>
        <div class="row mt-2">
            <div class="col-md-12">
                <div id="description">
                    Ürün Detayları : @Model.Description
                </div>
            </div>
        </div>
        <div class="row mt-2">
            <div class="col-md-12">
                <p>
                    Ürün Özellikleri:
                    @foreach (var item in Model.ProductProperties)
                    {
                        <h6> @item.Property.PropertyName : @item.Value </h6>
                    }
                </p>
            </div>
        </div>
        <div class="row mt-2">
            <div class="col-md-12">
                <button class="btn btn-success">
                    <a asp-controller="Cart" asp-action="Add" asp-route-id="@Model.Id" data-id="@Model.Id" class="btn btn-success">Sepete Ekle</a>
                </button>
            </div>
        </div>
    </div>
</div>
<div class="row mt-3">
    <div class="col-md-12">
        <div class="bd-example bd-example-tabs">
            <ul class="nav nav-tabs mb-3" id="pills-tab" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" id="pills-description-tab" data-toggle="pill" href="#pills-description" role="tab" aria-controls="pills-description" aria-expanded="true">Description</a>
                </li>
            </ul>
            <div class="tab-content" id="pills-tabContent">
                <div class="tab-pane fade show active" id="pills-description" role="tabpanel" aria-labelledby="pills-description-tab">
                    <p>Consequat occaecat ullamco amet non eiusmod nostrud dolore irure incididunt est duis anim sunt officia. Fugiat velit proident aliquip nisi incididunt nostrud exercitation proident est nisi. Irure magna elit commodo anim ex veniam culpa eiusmod id nostrud sit cupidatat in veniam ad. Eiusmod consequat eu adipisicing minim anim aliquip cupidatat culpa excepteur quis. Occaecat sit eu exercitation irure Lorem incididunt nostrud.</p>
                </div>
            </div>
        </div>
    </div>
</div>




